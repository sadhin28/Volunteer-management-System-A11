import React from 'react';
import useTitle from '../hooks/useTitle';

const Contact = () => {
    useTitle("Contact")
    return (
        <div>
            <h1>Contact</h1>
        </div>
    );
};

export default Contact;